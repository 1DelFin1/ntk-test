Для запуска:

cp .env.example .env

streamlit run app.py


## Тестирование
В папке samples есть примеры*, их можно вставить

\*это будут моковые тесты, для прогона через LLM в .env, в OPENAI_API_KEY нужно вставит апи ключ 